

c++类关于类静态成员和方法和类的普通成员和方法的关系以及区别

下面把静态属性(方法)称为类的属性(方法) ，普通属性(方法)称为对象的属性(方法)

#### 调用类成员和属性的几种方法


```cpp
/*

调用类成员和属性的几种方法:
    类名::属性或方法名
    对象.属性或对象名
*/
#include<cstdio>
#include<iostream>
using namespace std;
class Cat{
   
public:
    static int tot;
    static void Comemet(){
   
    tot=100;
    }
    static void Print(){
   
    cout<<tot<<endl;
    }
};
int Cat::tot=0;
int main(```


